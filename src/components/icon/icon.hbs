<svg class="icon{{#if additional_classes}} {{additional_classes}}{{/if}}">
	{{#each id}}
	<use xlink:href="#icon-{{ this }}"></use>
	{{else}}
	<use xlink:href="#icon-{{ id }}"></use>
	{{/each}}
</svg>
